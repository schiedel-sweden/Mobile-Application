/**
* Here we can define our variables which we want to access globally in the app
* ex: API endpoints or stat variables.
*/

export const API_URL_NO = 'https://jeremydanner.com/api';
export const API_URL_SV = '';
